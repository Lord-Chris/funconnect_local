import 'package:flutter/material.dart';

class PlannerMapView extends StatelessWidget {
  const PlannerMapView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Planner Map View"),
      ),
    );
  }
}
